{
  "name": "ring-election",
  "version": "0.0.1",
  "description": "Leader and followers algorithm to make partitioning easy.",
  "main": "ring/server.js",
  "engines": {
    "node": ">=10.0.0",
    "npm": ">=6.0.0"
  },
  "keywords": [
    "scheduler"
  ],
  "scripts": {
    "start-leader": "node ring/start.js",
    "test": "REPORTER=tap mocha --timeout 5000 **/*test.js",
    "demontest": "nodemon --exec \"npm test\"",
    "coverage": "REPORTER=tap nyc mocha  --timeout 5000 **/*test.js"
  },
  "author": "pioardi",
  "license": "ISC",
  "dependencies": {
    "@reactivex/rxjs": "^5.5.11",
    "compression": "^1.7.2",
    "express": "^4.16.2",
    "jsonwebtoken": "^8.3.0",
    "winston": "^3.1.0"
  },
  "devDependencies": {
    "expect": "^23.6.0",
    "mocha": "^5.2.0",
    "mock-require": "^3.0.2",
    "nyc": "^13.1.0",
    "supertest": "^3.3.0"
  }
}
